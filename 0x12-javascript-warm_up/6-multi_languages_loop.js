#!/usr/bin/node
//script to prints 3 lines: (like 1-multi_languages.js) but by using an array of string and loop.
const theArr = ["C is fun", "Python is cool", "JavaScript is amazing"];
for (let i = 0; i < theArr.length; i++) {
  console.log(theArr[i]);
}
